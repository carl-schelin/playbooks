#!/bin/ksh

# chkpolicy
# Carl Schelin
# Checks the openview installed policies

ADMIN=/opt/intrado/

if [[ -f $ADMIN/etc/opcpolicy.output ]]
then
  rm $ADMIN/etc/chkpolicy.output
fi

if [[ -f /opt/OV/bin/ovpolicy ]]
then
  /opt/OV/bin/ovpolicy -list > $ADMIN/etc/chkpolicy.output
fi

