---
- name: Install haproxy
  yum:
    name: haproxy
    state: latest

- name: Copy haproxy settings file 
  copy:
    src: ../files/{{ ansible_hostname }}.cfg
    dest: /etc/haproxy/haproxy.cfg

- name: restart haproxy
  systemd:
    name: haproxy
    daemon-reload: yes
    state: restarted

