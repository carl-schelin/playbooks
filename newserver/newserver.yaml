---
- hosts: "{{ tag }}"
  become: true
  become_user: root
# need this for sudoers
  gather_facts: yes

  roles:
    - chrony
    - firewall
    - ipa-client
    - ksh
    - mailx
    - snmpd
    - sshd
    - sudoers
    - sysadmin
    - tmproot
    - vmtoolsd
